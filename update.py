# So i need this to read the configs, see the event key, then do the following:
#   1) Check tba for the match schedule, then update the csv file if I need to
#   2) See if a storage file already exists, if it doesn't, make it